<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>405 Not Allowed</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #333;
      color: #fff;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
    }
    #timer {
      font-size: 48px;
      margin-top: 20px;
    }
  </style>
  <script>
    var timeLeft = 3; // Change this value to adjust the countdown

    function countdown() {
      if (timeLeft === 0) {
        window.location.href = "/";
      } else {
        document.getElementById("timer").innerHTML = timeLeft;
        timeLeft--;
        setTimeout(countdown, 1000);
      }
    }

    document.addEventListener("DOMContentLoaded", function(event) {
      countdown();
    });
  </script>
</head>
<body>
  <div class="container text-center">
    <h1 class="display-1 mb-4">405 Not Allowed</h1>
    <p class="lead">This page is not accessible.</p>
    <p class="lead">You will be redirected to the login page in</p>
    <div id="timer" class="display-4">3</div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
