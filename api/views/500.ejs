<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Under Maintenance</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            height: 100vh;
            background-color: #f8f9fa;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        .container {
            text-align: center;
        }

        .animation-container {
            width: 150px;
            height: 150px;
            margin: 0 auto; /* Horizontally center the gear */
            position: relative;
        }

        .gear {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            background: linear-gradient(90deg, #3498db, #e74c3c);
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%); /* Center the gear within its container */
            animation: rotate 2s infinite linear;
        }

        @keyframes rotate {
            0% {
                transform: translate(-50%, -50%) rotate(0deg);
            }
            100% {
                transform: translate(-50%, -50%) rotate(360deg);
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="display-4">Under Maintenance</h1>
        <p class="lead">We apologize for the inconvenience. This page is currently undergoing maintenance.</p>
        <div class="animation-container">
            <div class="gear"></div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
